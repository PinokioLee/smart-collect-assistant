# Smart Collect Assistant - 백엔드 의존성 (Python 3.11+)
# 핵심 (Phase 1: 엑셀 검증/병합 - 규칙 기반)
pandas==3.0.3
openpyxl==3.1.5
pydantic==2.13.4
python-dotenv==1.2.2

# 에이전트 오케스트레이션 (Phase 2)
langgraph==1.2.6
langchain==1.3.10
langchain-core==1.4.8
langchain-openai==1.3.2
openai==2.43.0

# API 서버 (Phase 3)
fastapi==0.138.0
uvicorn==0.49.0
python-multipart==0.0.32

# 테스트 / 클라이언트
pytest==9.1.1
httpx==0.28.1

# Gmail API 실제 발송 (EMAIL_SEND_MODE=gmail)
google-api-python-client>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0

# 선택: Langfuse 추적 (USE_LANGFUSE=true 일 때)
# langfuse

# 선택: FAISS 기반 RAG (운영 단계). Python 3.14 에서는 휠 부재 가능
# faiss-cpu
