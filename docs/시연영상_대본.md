# 시연 영상 대본 (5분 이내)

> 제출 파일명: `08079_이형진_시연영상.mp4` · MP4 · 5분 이내 · 편집 권장
> 핵심 원칙(가이드): **결과보다 '과정(추론 로그)'이 중요**. 단순 Tool Call이 아니라
> Planning / Tree of Thoughts / Self-Correction 로그를 확대해서 보여줄 것.

## 사전 준비
- 터미널 글자 크기 키우기(녹화 시 가독성). 다크 테마 권장.
- 명령 1개로 전체 흐름이 출력됨:
  ```bash
  set PYTHONUTF8=1
  python backend/cli.py demo
  ```
- (선택) UI 데모: `cd frontend && npm run dev` → 브라우저 업로드 화면

---

## 타임라인

| 시간 | 화면 | 나레이션(자막 대체 가능) |
|------|------|--------------------------|
| **0:00–0:30**<br>시나리오 소개 | 슬라이드 또는 터미널 상단 `[시나리오]` 배너 | "부서별 시스템 개선요청을 취합하는 상황입니다. 취합 요청 메일 1건과 부서 엑셀 3개를 입력해, 메일 분석부터 검증·자가교정·병합까지 자동으로 처리합니다." |
| **0:30–2:30**<br>**추론 로그(핵심)** | `python backend/cli.py demo` 실행 →<br>**'고수준 추론 로그' 구간을 화면 확대** | "여기가 핵심입니다. 단순 Tool 호출이 아니라 에이전트의 사고 과정이 로그로 남습니다.<br>① **PLAN**: 5단계 계획을 먼저 수립합니다.<br>② **Tree of Thoughts**: 검증 규칙 후보를 3개 생성(BRANCH×3)하고 coverage 게이트로 평가(EVALUATE)해 최적안을 선택(SELECT)합니다.<br>③ **VALIDATE**: 4종 규칙으로 결정론적 검증.<br>④ **Self-Correction**: 고칠 수 있는 오류만 CRITIQUE→REVISE 후 RE-VALIDATE. 날짜 `2026/06/05→2026-06-05`, 긴급도 `매우 급함→상`. **오류가 줄어들 때만 채택**합니다." |
| **2:30–3:30**<br>결과 확인 | 터미널 `[요약]`/`[생성 파일]` →<br>생성된 `*_merged.xlsx`, `*_error_report.xlsx` 열어 보여주기 | "결과입니다. 검출 오류 4행 중 1행을 자동 복구해, 정상 7행을 하나로 병합하고 잔여 오류는 보고서로 분리했습니다. 원본은 보존하고 결과만 별도 생성합니다." |
| **3:30–4:30**<br>**정량 검증** | 같은 실행의 `[정량 검증]` KPI 표 확대 | "주장이 아니라 수치로 증명합니다. 라벨링된 데이터셋에서 검출 **F1 100%**(오탐·미탐 0), 10회 반복 **재현성 표준편차 0**, 처리 **500행+/초**, 자가교정율 **100%**로 재작업 **60% 감소**, ToT가 양식 드리프트에서 **오탐 1건을 회피**했습니다." |
| **4:30–5:00**<br>성과 요약 | 핵심 수치 자막 4개 | "정리하면, LangGraph 멀티에이전트에 Tree of Thoughts와 Self-Correction을 더해 — 검출 F1 100%, 재현성 표준편차 0, 재작업 60% 감소를 달성했습니다. 감사합니다." |

---

## 편집 체크리스트
- [ ] 추론 로그(0:30–2:30)에서 **PLAN/ToT/Self-Correction 줄을 줌/하이라이트**
- [ ] 긴 대기·설치 구간은 컷 또는 1.5~2x 배속
- [ ] 핵심 수치(F1 100% / 표준편차 0 / 60%↓)는 자막·화살표 강조
- [ ] 5분 초과 금지(초과분 평가 제외). 결과·로그를 앞쪽에 배치
- [ ] 파일명 `08079_이형진_시연영상.mp4` 확인 후 제출

## 무료 편집 도구
CapCut · DaVinci Resolve · Clipchamp(Windows 기본) · QuickTime(Mac)
